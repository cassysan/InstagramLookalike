<div class="container">
  <div class="posts">
    <% @posts.each do |post| %>
      <div class="post">
        <%= cl_image_tag post.photo, height: 300, width: 300, crop: :fill, class: "post-img" %>
        <p><%= post.description %></p>
      </div>
    <% end %>
  </div>
</div>
